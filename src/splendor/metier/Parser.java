package splendor.metier;

/*import java.io.File;
import java.util.Scanner;

public class Parser {
    
    public static Carte[][] getDeck() throws Exception
    {
        Scanner sc = new Scanner( new File("Cartes.data") );

        //initialisation des deck

        int var = 0;
        while(sc.hasNextLine())
            var++;
        Carte[][] deck = new Carte[ 4 ][ var ];

        //création des decks

        sc = new Scanner( new File("Cartes.data") );
        int id = 0;
        String[] data;

        while ( sc.hasNextLine() )
        {
            data = sc.nextLine().split( " " );
            deck[  ][  ] = new Carte(  );
        }

        return deck;
    }
}*/